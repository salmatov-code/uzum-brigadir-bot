# Copy these variables into your Render environment or a local .env file
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
# The full service account JSON for Google credentials. Keep this secret.
# You can set this as a multi-line JSON string. Example: '{"type": "service_account", ...}'
GOOGLE_SERVICE_ACCOUNT_JSON='{}'
